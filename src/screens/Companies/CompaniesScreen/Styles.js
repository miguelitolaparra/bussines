import { StyleSheet } from "react-native"

const Styles = StyleSheet.create({
  content: {
    flex: 1,
  },
  btnContainer: {
    position: "absolute",
    bottom: 20,
    right: 20,
    opacity: 0.5
  },
})

export default Styles